<UserControl x:Name="userControl"
    x:Class="PhotoStore.CameraControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PhotoStore"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignWidth="200" Height="275" Background="Black">

    
        <StackPanel Height="275" VerticalAlignment="Top" CornerRadius="10" Background="Black">
            <Image x:Name="imgIcon" Width="190" Height="190" Source="{Binding imgSource,ElementName=userControl}" ></Image>
          
            <TextBlock x:Name="tblIconName" Margin="50,10" Text="{Binding tblIconName1, ElementName=userControl}" HorizontalAlignment="Center"  VerticalAlignment="Center" Foreground="White" FontSize="15" ></TextBlock>
        </StackPanel>

    
</UserControl>
